import AttendancePage from "../pages/Attendance";

// Simple re-export so components that import from ../widgets/Attendance
// (existing code) keep working. This avoids mass edits across the app.
export default AttendancePage;
