{
  "name": "hvms-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cross-env PORT=5000 npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "check-port": "cmd.exe /c \"netstat -aon | findstr :5000\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "cross-env": "^7.0.3"
  }
}
